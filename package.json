{
  "name": "petstore-mcp-demo",
  "version": "1.0.0",
  "description": "PetStore demo with intentional bugs for MCP + GitHub issues triage",
  "type": "module",
  "scripts": {
    "start": "node src/app.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}